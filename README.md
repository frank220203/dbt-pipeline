# dbt-pipeline
# MongoDB 드라이버 설치
pip install pymongo
# DuckDB 설치
pip install duckdb
# dbt-duckdb 어댑터 설치
pip install dbt-duckdb
# Pandas 설치
pip install pandas
# Airflow 설치
pip install "apache-airflow==2.10.3"
# pytest 설치
pip install pytest